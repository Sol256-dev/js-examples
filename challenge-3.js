let arr = [4,5,2,5,6,7];
let target = prompt("enter a number: ");
let tempStore;
let g;

function twoSum(x, y) {
    x.forEach(element => {
 
        // return element
        while (element + temp !== y) {

            element = tempStore;
        }
    });
}

twoSum(arr, target);